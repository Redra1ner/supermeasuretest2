package handler;


import javafx.scene.control.TextArea;
import javafx.stage.Stage;


//todo interfaces implementieren
public class FileHandler {
    public static void handleFile(Stage stage, TextArea imageInfos){
        /*
        FileChooser imageChooser = new FileChooser();
        File selectedFile = imageChooser.showOpenDialog(stage);
        Files file = FileType.handleMetaFile(selectedFile);
        String infoText = "image: "+file.getImageName()+"\n"+"resolution: "+file.getResolution()+" "+file.getUnit()+"\n"+"description: "+file.getDescription()+"\n"+"filetype: "+file.getFileType();
        imageInfos.setText(infoText);
        imageInfos.autosize();
        return file;
        */
    }
}
